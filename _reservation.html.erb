Reserved by <%= reservation.name %>

<% if user_signed_in? %>

	<% if reservation.user_id == current_user.id %>

<%= link_to "Edit", edit_book_reservation_path(reservation.book, reservation) %>
<%= link_to "Delete", book_reservation_path(reservation.book, reservation), method: :delete, data: { confirm: "Are you sure?" } %>

	<% end %>
<% end %>